# test_smoke.py — simple check that pytest itself runs
# This is the most basic test: if this passes, your test setup works.

def test_smoke():
    """Just checks that pytest can run a test at all."""
    assert True